
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "graphs_ellaevelynn"
version = "0.1.0"
description = "A Python package implementing Dijkstra's shortest path algorithm and graph utilities."
readme = "README.md"
authors = [{ name = "ellaevelynn" }]
requires-python = ">=3.8"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]